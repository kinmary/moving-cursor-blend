export const Cursor = () => {
  const size = 30;
  return (
    <div
      className="fixed top-0 left-0 bg-[yellow] rounded-full"
      style={{
        width: size,
        height: size,
      }}
    />
  );
};
